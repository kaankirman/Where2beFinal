<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ProgressBar
        android:id="@+id/pb_progress"
        android:layout_width="@dimen/progress_bar_size"
        android:layout_height="@dimen/progress_bar_size"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:indeterminate="true"
        android:indeterminateTint="@color/gradient_dark"/>
    <TextView
        android:id="@+id/tv_progress_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/progress_bar_tv_text_size"
        android:text="@string/progress_bar_text"
        app:layout_constraintStart_toEndOf="@id/pb_progress"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/pb_progress"
        android:layout_marginStart="@dimen/default_item_spacing"
        android:gravity="center"/>


</androidx.constraintlayout.widget.ConstraintLayout>